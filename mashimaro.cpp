#include <bits/stdc++.h>
using namespace std;

int main()
{
    double a;
    cin >> a;
    double side=sqrt(a*a/2);
    cout<< fixed << setprecision(2) << side*4<< endl;
    cout<< fixed << setprecision(2) << side*side;
}